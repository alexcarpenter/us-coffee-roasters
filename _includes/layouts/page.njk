{% extends "layouts/default.njk" %}

{% block intro %}
  <header class="c-intro">
    <h2 class="c-intro__heading">{{ title }}</h2>
    {% if description %}
      <p class="c-intro__description">{{ description | safe }}</p>
    {% endif %}
  </header>
{% endblock %}

{% block content %}{% endblock %}
